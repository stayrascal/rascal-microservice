spring:
  application:
    name: infra-config-client
  cloud:
    config:
      discovery:
        service-id: infra-config-server
        enabled: true
      profile: ${spring.profiles.active}
      label: master
      username: stayrascal
      password: stayrascal
server:
  port: 7888

management:
  context-path: /system
  security:
      enabled: false

