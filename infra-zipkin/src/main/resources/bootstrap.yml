spring:
  application:
    name: infra-zipkin
  cloud:
    config:
      discovery:
        service-id: infra-config-server
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 9411

