spring:
  application:
    name: infra-zipkin-server
  cloud:
    config:
      discovery:
        service-id: infra-config-server
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://192.168.99.100:8761/eureka/

server:
  port: 9411

